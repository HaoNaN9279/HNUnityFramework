---
sidebar_position: 14
---

# Entity 实体系统

🚧 **Stub** - 模块骨架已创建，内容待完善。

## 架构位置

Entity 模块位于 **Core 层** 的 `HN.Framework.Core` 程序集中，属于 Level.Logic 逻辑层的核心基础设施。具体位置见 [架构文档](/dev/architecture)。

## 概述

- `Entity.cs` — 纯数据实体基类，不依赖 Unity 运行时
- `EntityManager.cs` — 实体生命周期管理，负责创建、销毁、回收

## 状态

当前为 🚧 Stub 状态，仅包含骨架类定义，待后续迭代实现。
