# HNUnityFramework 架构 — 06：关卡层（逻辑 + 视图 + 动画架构）

> 本文档源自 `架构~/最终架构.md`，为拆分后的第七部分。
> 涵盖：L1-L10 关卡逻辑层、V1-V5 关卡视图层、动画系统五层架构。
> 最后更新：2026-07-14

---

## 九、关卡逻辑层

### L1 — MVC 框架

- **状态**：✅ 已实现
- **命名空间**：`HN.Framework.Core.Level.Logic`

**架构澄清：**

| 角色 | 定位 | 所在层 | 说明 |
|------|------|--------|------|
| **M (Model)** | 数据 + 业务逻辑 + 存储 | LogicModule (Core) | `Model<T>` 抽象类管理 `ModelUnit` 列表；`IReadOnlyModel<T>` 提供只读访问 + 变化通知 |
| **V (View)** | 显示逻辑 + 交互反馈 | ViewModule (Unity) | `ViewBase` 绑定并控制 `UnityEngine.GameObject`；由 Controller 驱动刷新；可通过 `IModelObserver` 订阅 Model 变化 |
| **C (Controller)** | 协调 M 和 V，处理用户输入 | LogicModule (Core) | `Controller` 抽象类管理 `ControllerUnit` 列表；注册于 `ControllerManager`；通过 V 接收输入，驱动 M 更新 |

**已有类型清单：**

```
Controller          — 抽象基类，持有 PooledList<ControllerUnit>，实现 ITickable + IReference
IController         — Controller 接口
ControllerUnit      — Controller 的逻辑单元
IControllerUnit     — ControllerUnit 接口
ControllerManager   — 注册/注销/Initialize/Tick/LateTick
Model               — 抽象基类，持有 PooledList<ModelUnit>
IModel              — Model 接口
ModelUnit           — Model 的逻辑单元
IReadOnlyModel<T>   — Value 属性 + OnValueChanged 事件
ReadOnlyModel<T>    — 默认实现（sealed class），SetValue() 仅在值变化时触发事件
```

**ControllerManager 生命周期：**
1. `RegisterController(controller)` — 注册到内部列表
2. `Initialize()` — 遍历所有 Controller，调用其 Initialize()
3. `Tick()` / `LateTick()` — 每帧遍历并驱动所有 Controller
4. Controller.Initialize() → 遍历所有 ControllerUnit，调用 Initialize()；然后 OnInitialize() 虚方法 → OnFirstFrame()

---

### L2 — HFSM

- **状态**：✅ 已实现
- **命名空间**：`HN.Framework.Core.Level.Logic`

**类型清单：**

```
IHFSM              — 状态机接口
HFSM               — 状态机实现类
IHFSMState         — 状态接口
HFSMState          — 状态基类
HFSMCompoundState  — 复合状态（支持子状态机）
HFSMTransition     — 转换（from → target，条件函数）
HFSMEntryState     — 入口状态（自动创建）
HFSMExitState      — 出口状态（自动创建）
```

**特性：**
- 完整复合状态支持（状态嵌套子状态机）
- 基于 `Func<bool>` 的条件转换
- ReferencePool 分配所有状态和转换（零 GC 分配）
- 死循环检测（`m_TempStates` 追踪本次更新执行过的状态）
- `Paused` 暂停支持
- Entry/Exit 状态自动管理生命周期

---

### L3 — Entity 系统

- **状态**：✅ 已实现
- **命名空间**：`HN.Framework.Core.Level.Logic.Entity`

```
Entity        — 纯数据实体基类（MemoryPack 可序列化用于网络同步，IReference 池复用）
EntityManager — 实体生命周期管理（Spawn / Despawn / GetEntity），由 GameWorld 持有
               通过 EventBus 发布 EntitySpawnedEvent / EntityDespawnedEvent
```

- Unity 侧：`EntityView` + `ViewFactory`（详见 V2）

---

### L4 — Sheet / Config 系统

- **状态**：✅ 已实现（Core + Unity + Editor 全链路）
- **命名空间（Core）**：`HN.Framework.Core.Level.Logic.Sheet`
- **命名空间（Unity）**：`HN.Framework.Unity.Capability.Sheet`

**Core 层：**

```csharp
// 配置表管理器
public interface ISheetManager
{
    IConfigTable<TKey, TRow> GetTable<TKey, TRow>(string tableName);
    void RegisterTable<TKey, TRow>(string tableName, IConfigTable<TKey, TRow> table);
    bool HasTable(string tableName);
}

// 泛型配置表接口
public interface IConfigTable<TKey, TRow>
{
    TRow Get(TKey key);
    bool TryGet(TKey key, out TRow row);
    TRow[] GetAll();
    int Count { get; }
    bool ContainsKey(TKey key);
}

// 默认实现，Dictionary O(1) 查表 + 数组零分配遍历
public class ConfigTable<TKey, TRow> : IConfigTable<TKey, TRow> { }

// MemoryPack 可序列化的资产引用
[MemoryPackable]
public partial struct AssetRef<T> : IEquatable<AssetRef<T>> { ... }

// MemoryPack 反序列化工具类
public static class ConfigLoader
{
    public static IConfigTable<TKey, TRow> LoadTable<TKey, TRow>(byte[] data, Func<TRow, TKey> keySelector);
}
```

**Unity 层：**
```
SheetManager         — ISheetManager 实现，支持 LoadAllFromBinary<TTables>(byte[])
ISheetRegistrar      — 由 Luban 生成的 Tables 实现，自动注册所有表
AssetRefExtensions   — AssetRef<T> 的 Addressables 加载扩展
```

**Editor（E7）：**
```
SheetEditorWindow    — 统一配置表编辑器窗口
TableModel           — 统一数据模型
SchemaProvider       — Excel schema 解析器
ExcelSourceParser    — Excel 文件解析器
ExcelSerializer      — 编辑后写回 .xlsx
SheetGrid            — UI Toolkit 网格视图
AssetRefCell         — 资产引用单元格
```

**Luban 协作模式：**

| 工具 | 负责 | 产出 |
|------|------|------|
| **Luban**（外部工具） | Excel/CSV → C# 代码生成 + 二进制数据导出 | 生成的 `Tables` 类 + `.bin` 数据文件 |
| **Sheet 编辑器**（框架 Editor） | Unity Editor 中编辑 Luban 数据表的资源引用字段 | ScriptableObject，存储数据行 → Addressables 资源地址映射 |

---

### L5 — 图数据模型

- **状态**：📋 规划中
- **命名空间**：`HN.Framework.Core.Level.Logic.Graph`

**核心类型（纯数据，MemoryPack 可序列化）：**

```
GraphData    — 图（节点集合 + 边集合）
GraphNode    — 节点（id, type, position, ports[], properties{}）
GraphEdge    — 边（source node + port → target node + port）
GraphPort    — 端口（direction: In/Out, type: string）
GraphGroup   — 节点编组（节点集合，用于视觉分组）
```

**领域扩展（继承 GraphData）：**
- `AnimationGraph`（L6）— 动画状态机图
- `BehaviorTreeGraph`（未来）— 行为树图
- `DialogueGraph`（未来）— 对话图

---

### L6 — 动画数据模型

- **状态**：📋 规划中
- **命名空间**：`HN.Framework.Core.Level.Logic.Animation`

```
AnimationGraph     — 动画状态机图（继承 GraphData，L5）
AnimState          — 动画状态（名称、动画片段列表、混合树配置）
BlendSpace1D       — 一维混合空间（参数 + 样本列表）
BlendSpace2D       — 二维混合空间（参数 X/Y + 样本网格）
SkeletonMask       — 骨骼遮罩（骨骼权重映射表）
IKConfig           — IK 配置（目标 + 链条参数）
AnimationEvent     — 动画事件（标准化时间点 + 事件类型 + 参数）
RootMotionConfig   — 根运动配置（提取轴向、缩放系数）
```

---

### L0 — GameplayTag（层级标签系统）

- **状态**：📋 规划中
- **命名空间**：`HN.Framework.Core.Level`

Level 层共享的轻量级层级标签，跨模块用于状态标记、条件匹配和权限控制。不归属于 Logic/View 子层，不绑定战斗系统。

```
GameplayTag           — 值类型(struct)，内部整数层级编码(如 0x010203→State.Stunned)，零GC
GameplayTagContainer  — 标签容器，Has/Add/Remove/MatchesQuery
TagQuery              — 查询表达式(And/Or/Not)，用于配置表条件匹配
```

- **数据来源**：Luban 配置表定义，L4 Sheet 运行时加载
- **特点**：启动时从配置表加载并冻结，无运行时注册，无字符串运行时开销

---

### L7 — 战斗数值体系（属性 / 效果 / Buff / 技能）

- **状态**：✅ 已实现
- **命名空间**：`HN.Framework.Core.Level.Logic.Combat`

基于 GAS 模式的数值组件体系。**核心四子模块（Attribute / Effect / Buff / Ability）全部实现，含完整 EditMode 单元测试**。**子模块可独立拆用**，适配不同游戏类型。**天然可插拔**（Level 层上层模块，无反向依赖，GameWorld 不持有其接口）。

**子模块依赖链：**

```
Attribute(独立) → Effect(依赖 Attribute) → Buff(依赖 Effect) → Ability(依赖 Buff + L0 GameplayTag)
                                                                           ↑ C7 EventBus(可选)
Propagation(规划中) → 调用 BuffSystem / EffectPipeline，自身无向下依赖
BuffBehaviour(规划中) → 由 BuffSystem 实例化，可选扩展
```

**不同游戏类型的子模块选择：**

| 类型 | GameplayTag | Attribute | Effect | Buff | Ability |
|-----|:-----------:|:---------:|:------:|:----:|:-------:|
| 休闲/叙事 | ✅可选 | ❌ | ❌ | ❌ | ❌ |
| RPG/MOBA/卡牌 | ✅ | ✅ | ✅ | ✅ | ✅ |
| 放置/数值养成 | ❌ | ✅ | ✅简化 | ❌ | ❌ |
| 平台跳跃(简化) | ❌ | ✅仅HP | ❌ | ❌ | ❌ |

**Attribute 子系统：**

```csharp
public class AttributeSet<TId>   // TId 泛化解耦 Entity：用 EntityId 需 Entity，用 int/string 不需要
{
    float GetBaseValue/FinalValue(AttributeType);
    ModifierHandle AddModifier(Modifier<TId>);  // ModOp: Add|Multiply|Override|MinCap|MaxCap
    void RemoveModifier(ModifierHandle);         // Priority 控制计算顺序
    event Action<AttributeType,float,float> OnValueChanged;
}
// 默认计算顺序：Override → Add(按Priority) → Multiply(按Priority) → MinCap → MaxCap
```

**DamagePipeline（四阶段伤害管线，核心公式可替换）：**

```
PreMigration(增伤/减伤判断) → DamageCalculate(默认加减法公式,可替换) 
  → PostMigration(护甲/分摊/反弹) → ApplyDamage(应用到目标)
```

**Effect / Buff / Ability 核心接口：**

```
IEffectPipeline:  Apply(EffectSpec) → ApplyResult        // Instant|Duration|Infinite
BuffSystem:        ApplyBuff/RemoveBuff/Tick              // 层叠: Single|Multi|Refresh|Extend
IAbilitySystem:    TryActivateAbility/ApplyEffect/RemoveEffect  // 标签条件预检 CanActivate
```

**Propagation — 通用效果传播机制（✅ 已实现）：**

三要素分离：**Strategy**（`IPropagationStrategy<TId>`，如何寻找传播目标：半径扫描、链式弹跳、距离网络等）、**Filter**（`IPropagationFilter<TId>`，目标筛选条件）和 **Effect**（命中目标后施加的效果）。三者独立可替换。

传播链上携带 **上下文对象**（`PropagationContext<TId>`，含当前跳数/已命中实体集合/强度倍率/自定义共享状态），支持每跳参数衰减和跨跳状态共享。

**PropagationSystem<TId>** 为核心执行器：接受 Strategy + Filter + Spec，执行完整传播循环（跳数验证→上下文创建→事件触发→循环找目标→过滤器检查→去重→效果施加→结果返回）。效果施加委托给 `IEffectPipeline<TId>`。自身不依赖 EventBus，仅通过 C# 事件（`OnPropagationStarted`/`OnTargetHit`/`OnPropagationComplete`）通知外部。

传统光环视为 Propagation 的特例（半径扫描 Strategy + 持续运行）。本模块只负责传播路径计算，效果施加委托给 BuffSystem/EffectPipeline。

**类型清单：** `PropagationSpec<TId>`（配置数据）、`PropagationContext<TId>`（运行时上下文）、`PropagationHandle`（追踪句柄）、`PropagationResult<TId>`（执行结果）、`IPropagationStrategy<TId>`（策略接口）、`IPropagationFilter<TId>`（过滤器接口）、`PropagationSystem<TId>`（执行器）

**测试覆盖：** 8 个 EditMode 单元测试（空策略/单跳/多跳限制/去重/强度衰减/过滤器/事件顺序/无效规格）

---

**BuffBehaviour — Buff 自定义行为（✅ 已实现）：**

定位为纯数据 BuffSpec 之外的可选扩展层。当 Buff 效果需要条件组合逻辑、跨系统交互或运行时状态判断时（如 RPG 中护盾期间技能额外触发效果、MOBA 中草丛移速增减、卡牌中装备修改抽牌规则、射击中换弹后增伤需跨事件组合），通过 Behaviour 实现自定义逻辑。

**IBuffBehaviour<TId>** 定义四个生命周期方法：`OnApply`（Buff 应用时）、`OnTick`（每帧 Tick）、`OnRemove`（移除时，区分 isExpired）、`OnStackChanged`（层数变更）。**BuffBehaviourContext<TId>** 承载运行时上下文（TargetAttributes/EffectPipeline/CustomParams）。

**集成方式：** `BuffSystem<TId>` 新增构造器，接受 `Func<int, IBuffBehaviour<TId>?>` 工厂委托。`BuffSpec<TId>` 新增 `BehaviourId`（int，0 表示无）和 `BehaviourCustomParams`（`Dictionary<string, Fixed64>?`）字段。无 Behaviour 时行为与修改前完全一致。

纯数值类和条件触发类效果优先走 AttributeSet.Modifier 和 EventBus+BuffSystem，Behaviour 仅在上游路径无法表达时使用。

**测试覆盖：** 7 个 EditMode 单元测试（OnApply/OnTick/OnRemove到期/OnRemove手动/OnStackChanged/无Behaviour异常/CustomParams传递）

**数据层：** 所有数据定义统一走 Luban + L4 Sheet，**不引入 ScriptableObject**。Unity 资源引用通过 `AssetRef<T>` 存 Addressables label 字符串，运行时异步加载：

```
Excel(tb_attribute/tb_ability/...) → Luban → C# struct(MemoryPackable) + .bin → L4 SheetManager
  → 运行时 world.SheetManager.GetTable<int,AbilityDefine>("tb_ability").Get(1001)
  → Unity 资源: AssetRef<T>.LoadAssetAsync() via Addressables
```

**依赖：** L0 GameplayTag（可选）+ C7 EventBus（可选）；**不依赖** ~~L1 MVC~~、~~L2 HFSM~~、~~L3 Entity~~（Entity 通过 `TId` 泛型可选）

---

### L8 — 任务 / 成就系统

- **状态**：📋 规划中
- **命名空间**：`HN.Framework.Core.Level.Logic.Quest`

事件驱动 + 条件组合模式。

```
Core 层：
  IQuestManager — RegisterQuest/StartQuest/CompleteQuest/GetQuestState
  QuestCondition 接口（组合模式）：
    SingleCondition / AndCondition / OrCondition / NotCondition
  QuestLine 数据模型：questId/quests[]/branchConditions[]/rewards[]

Unity 层：
  QuestDataSO — ScriptableObject 任务定义
  QuestManager — MonoBehaviour，注入 GameWorld
```

---

### L9 — 物品 / 背包 / 装备框架

- **状态**：📋 规划中
- **命名空间**（Core）：`HN.Framework.Core.Level.Logic.Inventory`
- **命名空间**（Unity）：`HN.Framework.Unity.Level.View.Inventory`
- **数据定义**：物品配置走 L4 Sheet + Luban，不引入 ScriptableObject

**与同级模块的协作关系：**

| 模块 | 关系 | 说明 |
|------|------|------|
| **L7 Combat**（强关联） | L9 调用 L7 | 装备属性效果 → L7 AttributeSet.Modifier；物品附带 Buff → L7 BuffSystem.ApplyBuff/RemoveBuff |
| **L3 Entity**（关联） | L9 通过 EntityId 持有 | L9 不直接引用 Entity，通过 EntityId 关联；掉落物用 L3 Entity + EntityView 展示 |
| **C7 EventBus**（可选） | L9 订阅/发布 | 触发型效果（攻击时概率触发）需通过事件总线监听战斗事件 |
| **C4 AssetManager**（依赖） | L9 加载资源 | 物品图标、模型、特效等通过 AssetRef\<T\> + Addressables 加载 |
| **V1 UI**（可选） | L9 提供数据 | 背包/装备/物品提示 UI 通过只读接口查询容器和物品数据 |

**Core 层子模块划分：**

- **物品实例** — 运行时物品对象（MemoryPack 可序列化，IReference 池复用），含堆叠数、耐久、随机词条等运行时状态
- **容器系统** — 背包/仓库/商店/合成材料栏等容器管理（创建/销毁/容量/增删查改），增量变更通知
- **装备系统** — 槽位约束校验、装备/卸下、自动处理 Buff 生命周期（装备时 ApplyBuff，卸下时 RemoveBuff）
- **物品规格绑定** — `ItemBuffSpec` 定义物品附带的 Buff 规格（激活方式、触发概率、参数覆盖），通过 `CustomParams` 实现独有效果差异化
- **合成配方**（可选）— 材料 + 配方 → 产出，配置表可定义
- **掉落表**（可选）— `IItemDropTable`，概率 + 数量 + 条件随机

**Unity 层划分：**

- **ContainerView / EquipmentView** — 绑定到 Entity 的 MonoBehaviour，与 EntityView 协作，负责 Core 层到场景的桥接
- **ItemWorldObject** — 场景中的可拾取物品（掉落/展示），依赖 V2 ViewFactory
- **UI 面板** — 背包面板、装备面板、物品提示等，归属 V1 UI 体系，通过只读接口访问 Core 层数据

---

### L10 — AI 行为框架

- **状态**：📋 规划中
- **命名空间**：`HN.Framework.Core.Level.Logic.AI`

```
Core 层：
  BehaviorTree 运行时：
    BTNode (基类)：Evaluate() → NodeState(Running/Success/Failure)
    CompositeNode：Sequence/Selector/Parallel
    DecoratorNode：Inverter/Repeater/Conditional/Cooldown
    ActionNode：具体行为节点（项目 Scripts 仓库实现）
    Blackboard：键值对黑板数据共享
    BehaviorTreeRunner：每帧驱动 BT 根节点
  IAIPerception — 感知接口
  INavigationAgent — 寻路接口
  AIConfig — 数据模型

Unity 层：
  AIAgentComponent — MonoBehaviour
  VisionSensor / AudioSensor — 传感器
  NavMeshAgentAdapter — NavMesh 适配
```

---

## 十、关卡视图层

### V1 — UI 系统运行时

- **状态**：✅ Phase 2 已完成
- **命名空间**：`HN.Framework.Unity.Level.View.UI`
- **对应能力接口**：C13 的 `IUIManager`

**7 层 Canvas 栈：**

| 层 | SortOrder | 说明 |
|----|:---------:|------|
| Background | 0 | 场景背景层 |
| Scene | 100 | 3D 场景 UI（HUD、血条） |
| UI | 200 | 主界面（HUD、菜单） |
| Popup | 300 | 弹窗、对话框 |
| Toast | 400 | 自动消失提示 |
| Guide | 500 | 新手引导覆盖层 |
| System | 600 | 系统级覆盖（加载画面、错误提示） |

**栈导航：**
- `Push(type)` / `Pop()` — 入栈/出栈，支持返回键处理
- `Show(type)` / `Hide(type)` — 不改变栈的显示/隐藏
- `PushAsync(path)` / `ShowAsync(path)` — Addressables 异步加载面板

**Panel 生命周期：**
```
Closed → Open() → OnEnterAnimation (LitMotion fade/slide/scale)
  → Opening → Opened → (Pause/Resume) → OnExitAnimation
  → Closing → Closed
```

**Panel 类型：**
- `UIPanel`（基类）✅ Phase 1
- `UIDialog`（模态弹框，阻挡下层交互）✅ Phase 2
- `UIToast`（自动消失提示，可选时长 + 排队机制）✅ Phase 2
- `UIGuide`（教程覆盖层，步骤驱动）✅ Phase 2

---

### V2 — ViewFactory + EntityView

- **状态**：✅ 已实现
- **命名空间**：`HN.Framework.Unity.Level.View`

```
ViewFactory  — 工厂类，从已加载的预制体创建 EntityView 实例
               · 同步 CreateView(prefab, position, rotation, entityDefId)
               · ReleaseView() 释放并销毁
               · RegisterPrefabMapping() 注册 EntityDefId → 地址映射
               · Prefab 缓存
EntityView   — MonoBehaviour 类，实体的视觉表示
               · EntityId / EntityDefId 属性
               · 通过 PropertyBinder (V3) 订阅数据变化
               · 生命周期：OnSpawned / OnDespawned（虚方法）
               · Initialize / Deinitialize（内部方法，由 ViewFactory 调用）
DefaultPropertyBinder — PropertyBinder 的 Dictionary 实现
```

---

### V3 — PropertyBinder

- **状态**：✅ 已实现（基础版本）
- **命名空间**：`HN.Framework.Unity.Level.View.Binding`

```csharp
public abstract class PropertyBinder
{
    public abstract void Bind<T>(IReadOnlyModel<T> source, Action<T> onValueChanged);
    public abstract void UnbindAll();
}
```

**扩展方向：**
- `CanvasPropertyBinder` — 自动绑定 UGUI 控件
- `ComponentPropertyBinder` — 绑定 Unity 组件属性

---

### V4 — 动画 Playable 运行时

- **状态**：📋 规划中
- **命名空间**：`HN.Framework.Unity.Level.View.Animation`

**功能：** 执行 L6 定义的动画数据模型，驱动 Unity Playable Graph。

- 构建 `PlayableGraph`：AnimationMixerPlayable, AnimationClipPlayable, AnimatorControllerPlayable
- 混合空间评估（1D/2D）
- 骨骼遮罩（AvatarMask）
- IK 求解器
- 根运动提取与应用
- `AnimationScriptPlayable` 自定义 Job 扩展点

---

### V5 — 音频播放组件

- **状态**：📋 规划中
- **命名空间**：`HN.Framework.Unity.Level.View.Audio`

```
AudioEmitter  — 3D 定位音频播放组件（添加到 GameObject）
                · 衰减曲线配置
                · 播放/停止/暂停 AudioEvent
AudioListener — 听者管理（跟随主相机）
```

---

## 十一、动画系统五层架构

### 11.1 分层概览

```
┌────────────────────────────────────────────────────────────────┐
│                    Unity Playable Graph                        │
│                         ▲                                      │
│  Layer 5: 运动层 (Motion Layer)                                │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │ 物理计算 · 根运动集成 · 碰撞解决 · 地面吸附 · IK 求解     │  │
│  └──────────────────────────────────────────────────────────┘  │
│                         ▲                                      │
│  Layer 4: 行为层 (Behavior Layer)                               │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │ 动画处理 · 混合树评估 · 状态机转换 · 运动数据生成         │  │
│  └──────────────────────────────────────────────────────────┘  │
│                         ▲                                      │
│  Layer 3: 请求层 (Request Layer)                                │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │ 请求过滤 · 优先级仲裁 · 双缓冲交换                         │  │
│  └──────────────────────────────────────────────────────────┘  │
│                         ▲                                      │
│  Layer 2: 决策层 (Decision Layer)                               │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │ 移动输入 · 战斗输入 · 上下文判断 · AnimationRequest 生成   │  │
│  └──────────────────────────────────────────────────────────┘  │
│                         ▲                                      │
│  Layer 1: 世界信息收集 (Knowledge Pool / Sensor)                │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │ 玩家位置 · 敌人位置 · 地形 · 武器状态 · 速度 · 输入状态   │  │
│  └──────────────────────────────────────────────────────────┘  │
└────────────────────────────────────────────────────────────────┘
```

### 11.2 数据流

```
KnowledgePool → DecisionLayer → AnimationRequest
  → RequestLayer (filter + prioritize) → BehaviorLayer (state machine + blend)
  → MotionLayer (physics + IK) → PlayableGraph → Unity Animator
```

---

> 继续阅读：[07-编辑器工具层与附录.md](./07-编辑器工具层与附录.md) | [索引页](./README.md)
