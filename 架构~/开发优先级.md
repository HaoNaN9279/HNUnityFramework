# HNUnityFramework 模块开发优先级

> 生成日期：2026-07-20（更新：E1 Editor UI 扩展系统全量完成、E9 资源质检与自动化构建已完成、大量模块状态同步为已完成状态）
> 基于最终架构文档（最终架构.md）和实际代码实现状态整理

---

## 状态标记说明

| 标记 | 含义 |
|:----:|------|
| ✅ | 已实现，可运行 |
| 🚧 | 有骨架/接口定义但不可用（throw NotImplemented / return null） |
| 📋 | 规划中，目录或文件完全不存在 |

---

## 全景总览

```
优先级         模块                                   依赖关系
────────────────────────────────────────────────────────────────────
P0 (阻塞) ─── D2/C1 MemoryPack 序列化 ────────────── ✅ 已完成
            └── C7 EventBus ──────────────────────── ✅ 已完成

P1 (高价值) ─ D3 Debug 基础设施 ──────────────────── ✅ 已完成
            ├── C14 输入系统 ──────────────────────── ✅ 已完成
            ├── C15 热更新+脚本 ──────────────────── HybridCLR + xLua(mod) ✅
            ├── C13/V1 UI 系统 (7层+红点) ──────────── 独立，框架核心能力
            ├── D2 HNRandom ──────────────────────── 确定性随机数（回放/测试/联网）✅
            └── C2 Debug 系统 ────────────────────── ✅ 已完成

P2 (工具链) ─ C3 Localization ──────────────────── 独立
            ├── L4 Sheet 运行时 ──────────────────── 依赖 C1 MemoryPack ✅
            ├── C16 物理抽象层 ────────────────────── 独立，刚需
            └── C17 摄像机管理 ────────────────────── 独立，刚需

P3 (子系统) ─ L3 Entity + V2 ViewFactory ─────────── 配合 MVC
            ├── C6 网络 + 同步深度扩展 ────────────── 依赖 C1 MemoryPack, D2
            ├── L7 战斗数值体系 ──────────────────── 依赖 L0(可选) + C7(可选)
            ├── L9 物品背包装备 ──────────────────── 依赖 L3 + C4 + L7(可选)
            ├── C11/V5 音频 (miniaudio) ──────────── 技术难度最高
            └── C12 过场动画 (Timeline) ──────────── Timeline 集成

P4 (动画/进阶) ─ L5 Graph + E2 Graph Editor
               ├── L6 Animation + V4 Playable
               ├── E1 Editor UI 扩展
               ├── L8 任务成就系统
                ├── L10 AI 决策管线
               ├── C19 场景流送
               └── C21 性能监控与埋点

P5 (补完) ── HNRenderPipeline, AddressablesExt
             ├── C18 数据安全
             ├── C20 昼夜天气
             ├── D2 FixedMathSharp ────────────────── ✅ 已完成（Core/Vendor DLL + Core 层自定义格式化器）
             └── E9 资源质检与自动化构建 ────────────────── ✅ 已完成（2026-07-20）
```

---

## 🥇 P0 — 核心阻塞项 ✅ 已完成

> 被最多其他模块依赖的基础设施。**以下两项均已完成，P0 不再有阻塞项。**

### 1. D2/C1 — MemoryPack 序列化 ✅

- **状态**：✅ **已完成（含遗留待办）**
- **实现内容**：
  - MemoryPack v1.21.4 NuGet DLL 引用（独立 asmdef `HN.Framework.Core.Vendor.MemoryPack`，`MemoryPack.Generator.dll` 置于 `Analyzers/` 但源码生成器未启用）
  - Core 包装器：`MemoryPackSerializer`（`Driver/Common/Serialization/`）
  - 序列化接口：`ISerializer` + `MemoryPackFormatterProvider`（`Capability/Serialization/`）
  - Unity 格式化器：`UnityFormatters`（Vector3/Quaternion/Color 等 16 种）+ `UnityFormattersInitializer`
  - FixedMathSharp 格式化器：`FixedMathSharpFormatters`（8 种定点数类型）
  - 网络消息格式化器：`MessageFormatters`（5 种消息类型）
- **测试**：`MemoryPackSerializerTests` + `MemoryPackFormatterTests` + `UnityFormattersTests` + `FixedMathSharpFormattersTests` 等
- **⚠️ 遗留待办 — 补齐缺失的手动格式化器（✅ 已完成）**
  - **完成内容**：为所有 5 个缺失手动格式化器的类型编写了 `MemoryPackFormatter<T>` 子类：
    - `Entity` → `EntityFormatter`（在 `Level/Logic/Entity/EntityFormatters.cs`）
    - `AssetRef<T>` → `AssetRefFormatter<T>`（开放泛型，`Level/Logic/Sheet/AssetRefFormatters.cs`）
    - `FrameInput` → `FrameInputFormatter`（`Capability/Network/FrameInputFormatters.cs`）
    - `PredictionReconcileData<T>` → `PredictionReconcileDataFormatter<T>`（开放泛型，`Capability/Network/Prediction/PredictionInputFormatter.cs`）
    - `PredictionInputBase` → `PredictionInputBaseFormatter`（同上）
  - **注册链整合**：所有格式化器已统一注册到 `FormattersInitializer.RegisterAll()`，调用链路为 `UnityFormattersInitializer.RegisterAll()` → `FormattersInitializer.RegisterAll()` → 各模块 `RegisterAll()`
  - **测试覆盖**：21 个新增测试用例全部通过（Entity 6 + AssetRef 3 + FrameInput 5 + Prediction 5 + 预存在测试 2）

### 2. C7 — 事件总线 ✅

- **状态**：✅ **已完成**
- **实现内容**：`IEventBus` 接口 + `EventBus` 实现（线程安全，Dictionary&lt;Type, List&lt;Delegate&gt;&gt; + lock）
- **测试**：`EventBusTests`（订阅/取消/发布/多线程安全）
- **位置**：`HN.Framework.Core.Capability.Event`

---

## 🥈 P1 — 高价值独立能力

> 日常游戏开发刚需，不阻塞其他模块但价值极高。

### 3. D3 — Debug 基础设施 ✅

- **状态**：✅ **全部已完成**
- **数据模型层**：`LogLevel` / `ILogChannel` / `LogEntry` / `IDebugHub` / `IDebugCommand` / `DebugHub`（`Driver.Common.Debug`）
- **Capability 运行时**：`DebugHub` / `DebugCommandRegistry` / `DebugModule`（`Capability.Debug`）— 模块注册、命令执行、ILogProvider 桥接
- **Unity 层**：`RuntimeDebugConsole`（UGUI `~` 键调试终端，命令历史、自动补全）
- **测试**：7 个测试文件（`DebugHubTests` / `CapabilityDebugHubTests` / `DebugCommandRegistryTests` / `DebugModuleTests` 等）

### 4. C14 — 输入系统 ✅

- **状态**：✅ **全部已完成**
- **核心实现**：`IInputManager` + `IInputBlocker`（Core 接口），`InputManager` + `InputBlocker` + `DeviceDetector` + `TouchInputAdapter` + `InputActionAssetLoader`（Unity 实现）
- **Editor 工具**：`InputDebugger`（`Editor/Input/`）✅ 已实现
- **依赖**：`com.unity.inputsystem`
- **待完善**：`SetControlScheme` 和 `LoadFromAsset`（Addressables）仍为占位实现，后续按需补完

### 5. C15 — 热更新与脚本系统 ✅

- **状态**：✅ 已完成
- **方案**：HybridCLR（C# 热更新）+ xLua（Mod 脚本）
- **依赖**：HybridCLR 包、xLua 包
- **实现**：Core 层抽象接口（IScriptEngine/IScriptMod/ModState/ScriptModConfig/IHotUpdateEntry），Unity 层 HybridCLRAdapter（AOT 元数据加载、热更 DLL 加载、入口发现与激活）+ LuaModManager（Mod 生命周期管理、LuaEnv 沙箱隔离、API 白名单机制），Editor 层构建管线处理器（AOT 元数据生成、原生库拷贝、安装验证）

### 6. C13/V1 — UI 系统

- **状态**：✅ Phase 2 已完成
- **Phase 1 内容**：`IUIManager` 接口（7 层 Canvas 栈导航）、`RedDotNode`（红点树父子聚合）、`UIManager`（Canvas 层级排序 + 栈导航）、`UIPanel`（生命周期状态机）、`UIAnimation`（LitMotion 预设动画：FadeIn/Out、SlideIn/Out、ScaleIn/Out）
- **依赖**：LitMotion（UI 动画引擎）
- **工作量**：**大**（框架最复杂的高层能力之一）
- **Phase 2 已完成**：UIDialog/UIToast/UIGuide 扩展面板类型、RedDotManager 红点管理器、Addressables 面板加载、独立 Level.View.UI 目录迁移

### 7. D2 — HNRandom

- **状态**：✅ **已完成**
- **依赖**：无（纯 C# 数学）
- **实现**：xorshift128+ 算法，支持种子设置与状态序列化（GetState/SetState）
- **位置**：`HN.Framework.Core.Driver.Common.Math`

---

## 🥉 P2 — 运行时工具链

> 提升开发效率的必备工具，部分依赖 P0/P1 完成。

### 8. C3 — 本地化系统

- **状态**：✅ 全部已完成（Core + Unity）
- **Core 实现**：`Locale` 值类型 + `StringTable` 字典查询 + `ILocaleProvider` 接口（完整数据层）
- **Unity 组件**：`LocaleManager` / `TextLocalizer` / `LocaleSelector` / `AssetLocalizer` — 已实现
- **工作量**：中 ✅ 已完成
- **建议**：独立模块，多语言标配。Core 层数据模型已就绪，完成 Unity 层即可端到端可用。

### 9. L4 — Sheet 运行时加载

- **状态**：✅ 全部已完成（Core + Unity + Editor）
- **Core 实现**：`ISheetManager` + `IConfigTable<TKey, TRow>` + `ConfigTable<TKey, TRow>`（Dictionary O(1) 查表 + 数组零分配遍历）+ `ConfigLoader`（MemoryPack 反序列化）+ `AssetRef<T>`（MemoryPack 可序列化资源引用）
- **Unity 实现**：`SheetManager` + `ISheetRegistrar` + `AssetRefExtensions`
- **Editor**：`SheetEditorWindow` + `SheetGrid` + `AssetRefCell` 等（完整 Excel 编辑流程）
- **依赖**：C1 MemoryPack ✅（已就绪）
- **建议**：全链路已通，Luban 协作模式待项目接入时验证。

### 10. C16 — 物理系统抽象层

- **状态**：✅ 已完成
- **Core 实现**：`IPhysicsWorld` + `IBody` + `ShapeDefinition` / `RaycastHit` / `CollisionEvent` / `PhysicsVector3` / `PhysicsQuaternion` / `PhysicsTypes`（8 个文件，完整物理抽象层）
- **Unity 实现**：`PhysXWorld`（245 行，封装 UnityEngine.Physics/Physics2D）+ `UnityBody`（IBody 的 MonoBehaviour 实现）
- **测试**：Core 层 7 个测试文件（ShapeDefinitionTests / RaycastHitTests / PhysicsVector3Tests / PhysicsTypesTests / IPhysicsWorldContractTests / IBodyContractTests / CollisionEventTests）+ Unity 层 2 个测试文件
- **工作量**：中
- **建议**：所有物理交互游戏的基础刚需，已完成。

### 11. C17 — 摄像机管理系统

- **状态**：✅ 已完成
- **Core 实现**：`ICameraManager` + `CameraPreset` + `CameraShakeProfile`（接口 + 完整数据模型）
- **Unity 实现**：`CameraManager`（181 行，Cinemachine Brain 包装器）+ `CameraHandle` + `CameraShake`
- **依赖**：`com.unity.cinemachine`（**强制依赖**，已声明于 `package.json`）
- **工作量**：小-中（全量依赖 Cinemachine，CameraManager 作为 Brain 的代码侧包装，
  不自研 Body/Aim/Noise/Blend/Collision 逻辑）
- **建议**：**3D 游戏核心体验模块**，已完成。

---

## P3 — 游戏功能子系统

> 大型子系统，价值高但工作量大，可并行推进。

### 12. L3/V2 — Entity 系统 + ViewFactory ✅

- **状态**：✅ **已完成**
- **实现内容**：
  - Core L3：`Entity`（MemoryPack 可序列化纯数据实体，IReference 池复用）+ `EntityManager`（Spawn/Despawn/GetEntity，EventBus 事件桥接）+ `EntitySpawnedEvent`/`EntityDespawnedEvent`
  - Unity V2：`EntityView`（concrete MonoBehaviour，OnSpawned/OnDespawned 生命周期，内建 DefaultPropertyBinder）+ `ViewFactory`（同步 CreateView/ReleaseView，预制体缓存映射）+ `DefaultPropertyBinder`（基于 Dictionary 的绑定管理）+ UIDialog/UIToast/UIGuide 扩展面板 + RedDotManager
  - GameWorld 集成：`GameWorld.EntityManager` 属性，构造初始化
  - 测试覆盖：Core 层 13 用例 + Unity 层 13 用例，共 26 用例全部通过
- **测试**：EntityTests (3)、EntityManagerTests (10)、EntityViewTests (6)、ViewFactoryTests (7)
- **依赖**：L1 MVC ✅、EventBus ✅、ReferencePool ✅、PropertyBinder ✅

### 13. C6 — 网络系统 + 网络同步深度扩展

- **编号**：C6（Core + Unity）
- **当前状态**：✅ Phase 1（FishNet 集成）+ ✅ C6.1 状态同步已完成 + ✅ C6.2 预测与校验已完成 + ✅ C6.3 实体权限
- **封装策略**：最大程度复用 FishNet 内置能力（NetworkBehaviour/SyncVar<T>/RPC/Spawn/Interest Management），不自建同步协议。预置 MonoBehaviour 组件（NetworkTransform/NetworkAnimator）由框架自行封装。

  | 位置 | 状态 |
  |------|:----:|
  | Core `INetworkManager` | ✅ 已实现 |
  | Core `SyncedModel<T>` / `SyncCollectionChange<T>` / `SyncDictChange<TKey,TValue>` | ✅ 已实现 — C6.1 |
  | Unity `FishNet*` 系列（FishNetNetworkManager/FishNetMessageBus/FishNetConnectionAdapter/FishNetSerializerAdapter） | ✅ 已实现并重构完成 |
  | Unity `NetworkEntityView` | ✅ 已实现，含 RegisterSyncedModel/ApplySyncValue 同步基础设施 |
  | Unity `FishNetSyncedList<T>` / `FishNetSyncedDictionary<TKey,TValue>` | ✅ 已实现 — C6.1 集合同步包装器 |

- **网络同步扩展：**
  - **C6.1 状态同步** ✅ — 基于 FishNet SyncVar<T>+SyncList+SyncDictionary，SyncedModel<T>→IReadOnlyModel<T>→View PropertyBinder(V3) 绑定链路
  - **C6.2 预测与校验** ✅ — 客户端预测(PredictedNetworkEntityView) + 服务器校验(PredictionManagerAdapter) + Lag Compensation(LagCompensationAdapter)
  - **C6.3 实体权限** ✅ — FishNet NetworkObject.Owner + Spawn/Despawn，与 EntityManager(L3) 集成

- **依赖**：C1 MemoryPack（Custom Serializer 注册）；D2 FixedMathSharp ⚠️（可选，仅 lockstep 需要）
- **建议**：FishNet 内置了大量同步基础设施，工作量较原方案（自建同步协议）大幅降低。C6.1 已按计划完成。

### 14. L7 — 战斗数值体系

- **状态**：✅ 已实现（六子模块全部完成）
- **设计核心**：
  - **GameplayTag（L0）**已独立为 Level 层共享基础设施，不绑定战斗系统
  - **AttributeSet<TId>** — 泛型属性集合，`TId` 解耦 Entity（可选依赖）
  - **Modifier** — 可扩展运算类型（Add/Multiply/Override/MinCap/MaxCap）+ 优先级控制计算顺序
  - **DamagePipeline** — 四阶段伤害管线（PreMigration → DamageCalculate → PostMigration → ApplyDamage），核心公式可被项目整体替换
  - **BuffSystem** — 层叠规则驱动（Single/Multi/Refresh/Extend）+ 生命周期回调
  - **IAbilitySystem** — 技能激活预检（GameplayTag 条件）+ 效果管道触发
  - **Propagation** — 效果传播机制（Strategy+Filter+PropagationSystem，接口骨架 + 执行器）
  - **BuffBehaviour** — Buff 自定义行为扩展（IBuffBehaviour + BuffSpec 集成，工厂委托注入）
- **数据层**：统一使用 Luban + L4 Sheet 系统（含 AssetRef\<T\> 处理 Unity 资源引用），不引入 ScriptableObject
- **依赖**：L0 GameplayTag（可选）、C7 EventBus（可选）；Entity 通过 `TId` 泛型可选
- **不依赖**：~~L1 MVC~~、~~L2 HFSM~~、~~L3 Entity~~
- **子模块可选性**：Attribute（独立）→ Effect（依赖 Attribute）→ Buff（依赖 Effect）→ Ability（依赖 Buff + GameplayTag），项目可按需取用
- **工作量**：大
- **建议**：RPG/动作/卡牌/MOBA 战斗刚需。分阶段：Attribute + DamagePipeline → Buff 系统 → Ability 技能系统。
- **六子模块（Attribute/Effect/Buff/Ability/Propagation/BuffBehaviour）全部完成**，含完整 EditMode 单元测试（共 ~88 用例）
  - Propagation：4 个 Runtime 文件 + 1 个测试文件（8 用例）
  - BuffBehaviour：1 个 Runtime 文件 + BuffSystem 集成 + BuffSpec 扩展 + 1 个测试文件（7 用例）

### 15. L9 — 物品/背包/装备框架

- **状态**：✅ 已实现
- **实现内容**：
  - **Item 模块**：`ItemCategory`/`EquipmentSlot`（枚举）、`ItemDef`（MemoryPackable struct 配置表行定义）、`ItemBuffSpec`（L7 Buff 桥接）、`ItemInstance`（IReference 池复用+MemoryPack 序列化）
  - **Container 模块**：`IContainer` 接口 + `Container`（定长数组实现，自动堆叠+事件通知）
  - **Equipment 模块**：`IEquipment` 接口 + `Equipment`（位掩码槽位校验+ApplyBuff/RemoveBuff 自动生命周期）
  - **序列化**：`ItemFormatters`（ItemInstance 自定义 MemoryPack 格式化器）
  - **Unity 层**：`ContainerView` / `EquipmentView`（MonoBehaviour 桥接组件）
- **数据定义**：配置表走 L4 Sheet + Luban，不引入 ScriptableObject
- **依赖**：L3 Entity（通过 EntityId 关联）、C4 AssetManager、C1 MemoryPack、L7 Combat（装备 Buff 效果调用 L7 BuffSystem）
- **测试**：45 个 EditMode 用例全部通过
- **工作量**：中
- **核心子模块**：物品运行时实例、容器系统、装备系统（含 Buff 生命周期管理）、物品规格绑定（ItemBuffSpec）、合成配方（可选）、掉落表（可选）
- **数据定义**：配置表走 L4 Sheet + Luban，不引入 ScriptableObject
- **依赖**：L3 Entity（通过 EntityId 关联）、C4 AssetManager、C1 MemoryPack、L7 Combat（可选，装备属性+Buff 效果需调用 L7）
- **工作量**：中-大
- **建议**：RPG/生存/卡牌核心系统。数据模型先行，运行时容器后续。装备属性效果优先集成 L7 Attribute 和 Buff 体系，不自建数值层。

### 16. C11/V5 — 音频系统

- **状态**：📋 完全未开始
- **依赖**：miniaudio（原生 C 库）、SteamAudio
- **工作量**：**极大**（C 原生库桥接 + 3D 音频）
- **建议**：**技术难度最高模块**，需 C# 包装原生 C 库。如项目音频需求简单，可暂用 Unity 内置 Audio。

### 17. C12 — 过场动画系统

- **状态**：✅ **Phase 1~4 全部完成**
- **依赖**：`com.unity.timeline`、C4 AssetManager、C7 EventBus、C3 Localization、C14 Input、C17 Camera
- **工作量**：大（Phase 1~4 全部完成）
- **设计摘要**：基于 Timeline 构建，不自建序列化引擎。核心创新 `CutsceneBindingResolver`（自定义 IExposedPropertyTable）实现角色名→GameObject 自动绑定，支持 EntityId/ScenePath/Tag/ActorComponent 四种解析。`CutsceneActorRegistry` 通过 CutsceneActor 生命周期主动注册实现 O(1) 查找，替代 GameObject.Find/FindObjectsByType 全场景扫描，保留 fallback 机制向后兼容。CutsceneManager 按优先级仲裁过场播放（Critical/Important/Normal/Background），播放时自动协调输入阻塞+摄像机接管+UI 控制。提供 5 种自定义 Track：Subtitle/Dialogue/GameState/Shake/TimelineEvent。
- **Phase 1（核心路径）**：ICutsceneManager + ICutscenePlayer 接口 → CutsceneManager + CutscenePlayer Unity 实现 → CutsceneBindingResolver（ScenePath/Tag 解析）→ CutsceneActor 组件 → 输入阻塞集成 → EditMode 测试
- **Phase 2（完善体验）**：EntityId 绑定模式（L3 集成）→ 摄像机接管（C17）→ 黑边+字幕 UI（C13）→ 队列+优先级管理
- **Phase 3（自定义 Track）**：SubtitleTrack → DialogueTrack → TimelineEventTrack → ShakeTrack + GameStateTrack
- **Phase 4（Editor）**：CutsceneBindingInspector → CutsceneEditorWindow → CutscenePreviewTool

---

## P4 — 动画系统与进阶模块

> 依赖链较长，部分依赖 P0/P1/P3 的基础模块。

### 18. L5/E2 — Graph 数据模型 + Editor

- **状态**：📋 完全未开始
- **核心**：`GraphData` / `GraphNode` / `GraphEdge` / `GraphPort`（MemoryPack 序列化）
- **依赖**：GraphView（Editor API）
- **工作量**：中-大
- **建议**：动画/行为树/对话编辑器的通用底层。先做 Core 数据模型（纯 C#），Editor 后续。

### 19. L6/V4 — Animation 数据模型 + Playable Runtime

- **状态**：📋 完全未开始
- **核心**：`AnimationGraph` / `BlendSpace1D/2D` / `SkeletonMask` / `RootMotionConfig`
- **依赖**：`com.unity.playables`、L5 Graph、L2 HFSM
- **工作量**：**极大**（游戏引擎最复杂子系统之一）
- **建议**：先完善 L2 HFSM（已实现），再逐步推进 L6 数据模型（纯 C#），最后 V4 Playable Runtime。

### 20. E1 — Editor UI 扩展系统

- **状态**：✅ 全量已实现（19 属性 + 扩展接口 + HNSmartInspector）
- **定位**：Odin-like 自定义 Inspector 增强（`[ReadOnly]`/`[Button]`/`[ShowIf]`/`[Title]`/`[InfoBox]`/`[PropertyOrder]` 等）
- **工作量**：中-大
- **实现方案**：
  - **运行时属性**：属性定义放 `Runtime/HN.Framework.Unity/EditorUI/Attributes/`，继承 `PropertyAttribute`，用户脚本直接 `using` 使用
   - **HNSmartInspector**：`[CustomEditor(typeof(MonoBehaviour), editorForChildClasses: true)]` 全局注册，自动覆盖无专属 Editor 的 MonoBehaviour
  - **扩展机制**：`IPropertyDescriptor` + `IClassDescriptor` + `DescriptorRegistry` 三件套，项目可注册自定义属性处理器
  - **双轨绘制**：简单属性走 `PropertyDrawer`（默认 Inspector 也生效），复杂属性走 HNSmartInspector 绘制链
- **依赖**：UnityEditor 内建 API，无新增外部依赖
- **实现范围（全量已完成）**：19 个属性全部实现 —
  - 交互类：ReadOnly、Button、EnableIf、DisableIf
  - 可见性：ShowIf、HideIf、ShowInInspector、HideInInspector
  - 装饰类：Title、InfoBox、PropertyOrder
  - 验证类：Required、ValidateInput
  - 布局类：FoldoutGroup、BoxGroup、TabGroup、HorizontalGroup、VerticalGroup
  - 动作类：OnValueChanged
- **扩展接口**：IPropertyDescriptor（字段级）+ IClassDescriptor（类级）+ DescriptorRegistry（注册中心）全部实现

### 21. L8 — 任务/成就系统

- **状态**：✅ 已实现
- **核心**：事件驱动追踪 + 条件组合模式。六子模块：Quest 核心 / Achievement 核心 / 条件引擎 / 奖励系统 / 任务链 / QuestManager 统一管理器
- **Core 子模块**：
  - **Quest 核心** — 5 态状态机（Locked / Active / Completed / Claimed / Failed），QuestDef 配置表行，QuestInstance 运行时实例
  - **Achievement 核心** — 4 态状态机（Hidden / Revealed / Completed / Claimed），AchievementDef 配置表行，AchievementInstance 运行时实例
  - **条件引擎** — CounterCondition（累计计数，绑定 EventBus 事件）、StateCondition（状态检查）、CompositeCondition（And/Or/Not 组合），ConditionEvaluator 管理订阅与评估
  - **奖励系统** — RewardDef 配置表行，IRewardHandler 开放接口，项目实现具体发放逻辑
  - **任务链** — QuestChainDef 线性/简易分支推进
  - **QuestManager** — 统一管理器，发布 QuestStateChanged / QuestProgressUpdated / AchievementStateChanged / RewardClaimed 事件
- **Unity 层**：QuestManagerBridge（GameWorld 注入）、可选 UI 桥接组件
- **数据定义**：全部走 L4 Sheet + Luban（tb_quest / tb_quest_condition / tb_quest_chain / tb_achievement / tb_reward / tb_achievement_category），不引入 ScriptableObject
- **依赖**：C7 EventBus（必选）+ C8 Storage（可选）+ L9 Inventory（可选）+ L7 Combat（可选）+ L0 GameplayTag（可选）+ C3 Localization（可选）
- **工作量**：中-大
- **建议**：内容驱动型游戏核心系统。先实现 Quest 核心 + 条件引擎，再补完 Achievement 和任务链。

### 22. L10 — AI 决策管线

- **状态**：📋 规划完成，待实现
- **核心**：`IDecisionStrategy` 策略接口体系 + `DecisionPipeline` 管线编排 + `KnowledgePool` 知识池 + `ActionExecutor` 动作执行
- **内置策略**：BehaviorTree / FSM（包装 L2 HFSM）/ Utility System / GOAP / HTN / DecisionTree / FuzzyLogic / Scripted
- **依赖**：L2 HFSM（FSMStrategy 包装）、L3 Entity（Agent 关联）、L0 GameplayTag（条件判断）、C7 EventBus（感知事件）、C10 ObjectPool（节点池化）、C15 Scripting（ScriptedStrategy）、L4 Sheet（AI 配置）
- **可选依赖**：L5 Graph（策略编辑器数据模型）、E2 GraphEditor（策略可视化编辑）
- **工作量**：大
- **建议**：先实现核心框架（IDecisionStrategy + DecisionPipeline + KnowledgePool + 1-2 种策略），逐步添加更多策略和编辑器支持。

### 23. C19 — 场景流送与大世界管理

- **状态**：📋 规划完成，待实现
- **核心接口**：`ISceneStreamingManager` + `WorldPartitionConfig` + `StreamingVolume`
- **依赖**：Addressables
- **工作量**：中-大
- **建议**：仅开放世界/大地图游戏需要。非必需模块，按需推进。

### 24. C21 — 性能监控与埋点

- **状态**：📋 规划完成，待实现
- **核心**：`PerformanceCounter` / `ITelemetryProvider` / `RuntimeProfiler` / `TelemetryHub`
- **工作量**：中
- **建议**：生产环境必备，可增量推进。先 Core 接口+基础性能计数，埋点和 Editor 工具按需补充。

---

## P5 — 补完与远期

> 不阻塞任何其他模块，随时可做或按需接入。

### 25. C18 — 数据安全

- **状态**：📋 规划完成，待实现
- **核心**：AES/XOR 加密、`AntiCheatFloat/Int` 混淆数值、存档 HMAC 签名
- **工作量**：小-中
- **建议**：联网/付费游戏存在安全风险，后期按需加入。独立性强。

### 26. C20 — 昼夜/天气时间系统

- **状态**：📋 规划完成，待实现
- **核心接口**：`IGameClock` + `DayNightCycleConfig` + `WeatherTransitionConfig`
- **工作量**：中
- **建议**：沉浸式 3D 游戏加分项。独立性强，随时可接入。

### 27. E9 — 资源质检与自动化构建

- **状态**：✅ **已实现**（2026-07-20）
- **核心设计**：
  - `AssetRuleRegistry` 按 Importer 类型分组的规则注册中心，`IAssetRule` 开放接口供项目扩展自定义规则和自定义 Importer
  - 框架预设通用规则（MissingScript / NamingConvention / ReferenceIntegrity / Addressables），不预设纹理/模型/音频等业务相关规则
  - `BuildPipelineOrchestrator` 构建编排 + 质检门禁 + 版本管理 + 构建报告
  - `RedundancyScanner` 引用图分析 + 冗余清理
  - `VersionManager` 版本号管理 + Git tag + BuildManifest
  - `AssetValidatorWindow` / `RedundancyCleanerWindow` / `BuildPipelineWindow` / `BuildReportWindow` 四个 EditorWindow 界面
  - `BuildReportGenerator` JSON/Markdown 双格式报告生成
- **位置**：`Editor/BuildPipeline/`，命名空间 `HN.Framework.Editor.BuildPipeline`
- **依赖**：UnityEditor 内建 API，无新增外部依赖
- **测试**：38 个 EditMode 单元测试全部通过（规则框架 9 + 预设规则 14 + 校验/冗余 15 + 管线 14）

### 28. D2 — FixedMathSharp ⚠️ 可选模块（第三方定点数库）

- **状态**：✅ **已完成**（DLL 引用 + Core 层自定义 MemoryPack 格式化器）
- **方案**：**自编译 FixedMathSharp.dll（.NET Standard 2.1）+ precompiledReferences 引用 + Core 层自定义 MemoryPack 格式化器**
- **实现方式**：
  - `HN.Framework.Core.Vendor.FixedMathSharp.asmdef` 通过 `precompiledReferences: ["FixedMathSharp.dll"]` 引入预编译 DLL
  - `FixedMathSharpFormatters.cs` 在 `Capability/Serialization/` 下，提供 8 个 `MemoryPackFormatter<T>`（Fixed64、Vector2d/3d/4d、FixedQuaternion、Fixed4x4、FixedBoundBox、FixedBoundSphere）
  - `FormattersInitializer.RegisterAll()` 批量注册所有格式化器
  - `HNFixedPoint.cs` 通过 `using HNFixedPoint = FixedMathSharp.Fixed64;` 提供类型别名兼容
- **测试**：`FixedMathSharpFormattersTests.cs`（序列化往返测试，17 个用例全部通过）
- **工作量**：中（自编译 DLL + 8 个自定义格式化器，~400 行）
- **必要性**：❌ **非必须**。仅 lockstep 帧同步场景需要（RTS、格斗、竞速等），默认状态同步（FishNet SyncVar/SyncList/Prediction）使用 `float` 无需定点数

### 29. P5 遗留项

| 优先级 | 模块 | 编号 | 当前状态 | 说明 |
|:------:|------|:----:|:--------:|------|
| 29 | 自定义渲染管线 | D4 | 🚧 有占位文件 | 可选优化。框架设计要求 SRP 不启用时一切正常，所以这是纯优化项。ShaderLibrary 已就位。 |
| 30 | Addressables 扩展 | E8 | 🚧 有骨架类 | GroupPresets 数据模型已有，补完 Editor 工具即可。不阻塞任何人。 |

---

## 建议推进策略

### 阶段一：打地基（P0）✅ 已完成
```
MemoryPack 序列化 (C1) ──→ EventBus (C7) ──→ D3 Debug 基础设施 ──→ C2 Debug 系统
```
- **状态**：✅ **全部完成**
- **产出**：高性能序列化基础、模块间事件通信、结构化日志系统、运行时调试终端

### 阶段二：核心能力（P1 + P2）✅ 已完成
```
✅ C14 输入系统              ─── 已完成
✅ C15 热更新 HybridCLR      ─── 已完成
✅ D3/C2 Debug 全套          ─── 已完成
✅ UI 系统 (C13+V1)         ✅ Phase 2 已完成
✅ 物理系统 (C16)            ─── 已完成
✅ 摄像机 (C17)              ─── 已完成
✅ Sheet 运行时 (L4)         ─── 已完成（Core + Unity + Editor 全链路）
✅ HNRandom                  ─── 已完成
✅ C3 本地化 (Core + Unity)  ─── Core 层 + Unity 层全部已完成
```
- **状态**：✅ **全部完成（含 Unity 侧本地化）**
- **产出**：框架在项目中可投入使用的核心功能集 + 全套工具链

### 阶段三：游戏子系统（P3）🔄 推进中
```
✅ Entity + ViewFactory (L3+V2)         ─── 已完成
✅ 网络系统 (C6) + 同步深度             ─── 已完成（含 C6.1 状态同步 + C6.2 预测与校验）
战斗数值体系 (L7)         ─── 依赖 L0 + C7（均为可选）
物品背包 (L9)             ─── 依赖 L3 + C4 ✅
音频系统 (C11/V5)          ─── 技术攻坚
过场动画 (C12)             ─── ✅ 已完成（Phase 1~4 全部完成）
```
- 预计耗时：5-8 周（音频和过场并行攻坚）
- 产出：完整的游戏开发框架能力

### 阶段四：动画与进阶工具（P4 + P5）
```
Graph + Graph Editor (L5+E2)
Animation + Playable (L6+V4)
Editor UI 扩展 (E1) ✅ 已完成 — 19 属性 + 扩展接口体系 + HNSmartInspector
任务成就系统 (L8)
AI 决策管线 (L10)
场景流送 (C19)
性能监控 (C21)
数据安全 (C18)
昼夜天气 (C20)
资源质检 + 自动化构建 (E9) — 规则注册中心模式，预设通用规则，项目扩展自定义规则和 Importer
渲染管线 / Addressables 扩展
FixedMathSharp ─────────────── ✅ 已完成（Core/Vendor DLL + Core 层自定义格式化器）
```
- 预计耗时：4-6 周（可并行推动）
- 产出：进阶能力（可视动画编辑器、高性能动画系统、AI 框架）+ 运维工程化

---

## 附录：各模块当前实现状态速查

| 编号 | 模块 | Core | Unity | Editor | 总状态 |
|:----:|------|:----:|:-----:|:------:|:------:|
| D1 | GameWorld | ✅ | — | — | ✅ |
| D2 | ITickable/IReference | ✅ | — | — | ✅ |
| D2 | HNLogicTime | ✅ | — | — | ✅ |
| D2 | ReferencePool+PooledCollections | ✅ | — | — | ✅ |
| D2 | ObjectPool/PoolBase | ✅ | — | — | ✅ |
| D2 | AsyncLoadHandle | ✅ | ✅ | — | ✅ |
| D2 | Json/JsonObject | ✅ | — | — | ✅ |
| D2 | **MemoryPack 核心** | ✅ | — | — | ✅ |
| D2 | **HNRandom** | ✅ | — | — | ✅ |
| D2 | **FixedMathSharp ⚠️可选** | ✅ | — | — | ✅ (DLL 引用 + Core 层自定义格式化器) |
| D2 | **IEventBus** | ✅ | — | — | ✅ |
| D3 | **LogLevel/ILogChannel/LogEntry** | ✅ | — | — | ✅ |
| D3 | **IDebugHub/IDebugCommand/DebugHub** | ✅ | — | — | ✅ |
| D3 | **DebugHub Capability 运行时** | ✅ | — | — | ✅ |
| D4 | GameWorldDriver | — | ✅ | — | ✅ |
| D4 | AssetManager+IAssetOperator | — | ✅ | — | ✅ |
| D4 | Addressables/Resources/AssetDatabaseOp | — | ✅ | — | ✅ |
| D4 | AssetCacheItem | — | ✅ | — | ✅ |
| D4 | GameObjectPool 系列 | — | ✅ | — | ✅ |
| D4 | HNDictionary/SerializableDictionary | — | ✅ | — | ✅ |
| D4 | JsonData | — | ✅ | — | ✅ |
| D4 | UnityLogProvider | — | ✅ | — | ✅ |
| D4 | UnityTimeProvider | — | ✅ | — | ✅ |
| D4 | UnityCoroutineProvider | — | ✅ | — | ✅ |
| D4 | GlobalSettings | — | ✅ | — | ✅ |
| D4 | **HNRenderPipeline+ShaderLibrary** | — | 🚧 | — | 🚧 |
| C1 | **MemoryPack 序列化系统** | ✅ | ✅ | — | ✅ |
| C2 | **Debug 系统** | ✅ | ✅ | 📋(E3) | ✅ |
| C3 | **本地化系统** | ✅ | ✅ | 📋(E4) | ✅ |
| C4 | IAssetManager | ✅ | ✅ | — | ✅ |
| C5 | ILogProvider | ✅ | ✅ | — | ✅(待扩展) |
| C6 | **网络系统** | ✅ | ✅ | — | ✅ |
| C7 | **EventBus** | ✅ | — | — | ✅ |
| C8 | IStorageProvider | ✅ | — | — | ✅ |
| C9 | ProcedureManager | ✅ | — | — | ✅ |
| C10 | ObjectPoolManager | ✅ | — | — | ✅ |
| C11 | **音频系统** | 📋 | 📋 | 📋(E5) | 📋 |
| C12 | **过场动画系统** | ✅ | ✅ | ✅(E6) | ✅ Phase 1~4 全部完成 |
| C13 | **UI 系统** | ✅(Phase 2) | ✅(Phase 2) | — | ✅ Phase 2 |
| C14 | **输入系统** | ✅ | ✅ | ✅(InputDebugger) | ✅ |
| C15 | **HybridCLR + xLua(mod)** | ✅ | ✅ | ✅ | ✅ |
| C16 | **物理系统抽象层** | ✅ | ✅ | — | ✅ |
| C17 | **摄像机管理系统** | ✅ | ✅ | — | ✅ |
| C18 | **数据安全** | 📋 | 📋 | — | 📋 |
| C19 | **场景流送** | — | 📋 | — | 📋 |
| C20 | **昼夜天气时间** | 📋 | 📋 | — | 📋 |
| C21 | **性能监控与埋点** | 📋 | 📋 | 📋 | 📋 |
| L0 | **GameplayTag 标签系统** | ✅ | — | — | ✅ |
| L1 | MVC 框架 | ✅ | — | — | ✅ |
| L2 | HFSM | ✅ | — | — | ✅ |
| L3 | **Entity 系统** | ✅ | ✅(V2) | — | ✅ |
| L4 | **Sheet/Config 系统** | ✅ | ✅ | ✅(E7) | ✅ |
| L5 | **Graph 数据模型** | 📋 | — | 📋(E2) | 📋 |
| L6 | **Animation 数据模型** | 📋 | 📋(V4) | 📋(E2) | 📋 |
| L7 | **战斗数值体系（可拆分子模块）** | ✅ | — | — | ✅ |
| L8 | **任务成就系统** | ✅ | ✅ | 📋 | ✅ |
| L9 | **物品背包装备** | 📋 | — | — | 📋 |
| L10 | **AI 决策管线** | 📋 | 📋 | 📋(E2) | 📋 |
| V1 | **UI 运行时** | — | ✅(Phase 2) | — | ✅(Phase 2) 独立 Level.View.UI 目录 + Addressables + 动画集成 |
| V2 | **ViewFactory+EntityView** | — | ✅ | — | ✅ |
| V3 | PropertyBinder | — | ✅ | — | ✅ |
| V4 | **Animation Playable Runtime** | — | 📋 | — | 📋 |
| V5 | **Audio Playback** | — | 📋 | — | 📋 |
| E1 | **Editor UI 扩展** | — | ✅ | ✅ | ✅ |
| E2 | **Graph Editor（含 AI 策略编辑器）** | — | — | 📋 | 📋 |
| E3 | **Debug Hub Editor** | — | — | 📋 | 📋 |
| E4 | **Localization Editor** | — | — | 📋 | 📋 |
| E5 | **Audio Editor** | — | — | 📋 | 📋 |
| E6 | **Cutscene Editor** | — | — | 📋 | 📋 |
| E7 | Sheet Editor | — | — | ✅ | ✅ |
| E8 | ObjectPoolViewer | — | — | 📋 | 📋（空目录） |
| E8 | AddressablesExtensions | — | — | ✅ | ✅（GroupPresets + Updater） |
| E8 | HNDictionaryDrawer | — | — | ✅ | ✅ |
| E8 | HNUndoableObject | — | — | ✅ | ✅ |
| E8 | Settings Provider | — | — | ✅ | ✅ |
| E9 | **资源质检与自动化构建**（规则注册中心 + 预设通用规则 + 构建编排 + 版本管理 + 冗余检测 + 构建报告） | — | — | ✅ | ✅ |

**总结**：76 个模块项中，约 **53 个是 ✅ 已实现**（含 C13/V1 UI Phase 2 + C16/C17/L4 + D2 FixedMathSharp + L7 Propagation/BuffBehaviour 完成 + E1 EditorUI 全量实现），约 **2 个是 🚧 骨架/搭建中**（D4 HNRenderPipeline、E8 AddressablesExtensions），约 **21 个处于 📋 待开始**。
