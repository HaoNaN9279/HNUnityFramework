# update-docs-site - Work Plan

## TL;DR (For humans)

**What you'll get:** docs-site 三部分文档（使用指南、API 文档、开发文档）全面更新为新的三层架构——GameWorld 驱动模式、三程序集结构、CapabilityModule 服务接口，并为所有 Stub 模块添加占位页面。

**Why this approach:** 四波并行更新——先修关键文件（core/quick-start/api/architecture），再修高优（MVC/对象池/Procedure），然后低优（命名空间更新），最后补新。每波内部文件可并行处理，最大化效率。

**What it will NOT do:** 不修改 C# 源代码（命名空间问题仅记录不修复），不修改站点配置，不为 Stub 编造细节。

**Effort:** Medium（21 个文档文件，4 波更新）
**Risk:** Low — 纯文档修改，不影响运行时代码
**Decisions to sanity-check:** 新增 6 个 Stub 占位文档是否覆盖了你预期的所有模块？

Your next move: 审批通过，或使用 `$start-work` 启动执行。详细执行计划见下。

---

> TL;DR (machine): Medium effort, Low risk, 21 files in 4 waves, pure doc update to match 最终架构.md

## Scope
### Must have
- 重写 guide/core.md（GameWorld + GameWorldDriver + 固定步长逻辑帧）
- 重写 guide/quick-start.md（GameWorldDriver 继承模式的代码示例）
- 重写 guide/index.md（三层架构概述 + 新模块导航表）
- 更新 guide/install.md（新目录结构验证图）
- 更新 guide/mvc.md / object-pool.md / procedure.md（`world.XxxManager` 实例化模式）
- 更新 guide/reference-pool.md / hfsm.md / serialize.md / utils.md / asset-manager.md（命名空间/using 语句）
- 新增 guide 占位文档：capability.md, entity.md, view-factory.md, network.md, sheet.md, rendering.md
- 重写 api/index.md（三程序集按架构层分类）
- 重写 dev/architecture.md（基于 最终架构.md 浓缩版，~200 行）
- 更新 dev/code-standard.md（命名空间表）

### Must NOT have (guardrails, anti-slop, scope boundaries)
- ❌ 不修改 C# 源代码
- ❌ 不修改 docusaurus.config.ts / sidebars（均为 autogenerated）
- ❌ 不修改 docfx/ 或 scripts/
- ❌ 不为 Stub 文档编造不存在的 API 细节（仅概述+架构定位+🚧标记）
- ❌ 不删除现有文档文件

## Verification strategy
> Zero human intervention - all verification is agent-executed.
- Test decision: none（纯文档，无测试框架）
- Evidence: .omo/evidence/task-<N>-update-docs-site.md（每 todo 的变更摘要）

## Execution strategy
### Parallel execution waves
- **Wave 1** (🔴): 4 critical rewrites — guide/core.md, guide/quick-start.md, api/index.md, dev/architecture.md
- **Wave 2** (🟡): 6 high-priority updates — guide/index.md, guide/install.md, guide/mvc.md, guide/object-pool.md, guide/procedure.md, dev/code-standard.md
- **Wave 3** (🟢): 5 low-priority namespace updates — reference-pool.md, hfsm.md, serialize.md, utils.md, asset-manager.md
- **Wave 4** (➕): 6 new placeholder docs — capability.md, entity.md, view-factory.md, network.md, sheet.md, rendering.md

### Dependency matrix
| Todo | Depends on | Blocks | Can parallelize with |
| --- | --- | --- | --- |
| 1-4 (Wave1) | nothing | Wave2 (need new API names) | each other |
| 5-10 (Wave2) | Wave1 | nothing | each other |
| 11-15 (Wave3) | nothing | nothing | each other + Wave4 |
| 16-21 (Wave4) | nothing | nothing | each other + Wave3 |

## Todos
<!-- APPEND TASK BATCHES BELOW THIS LINE WITH edit/apply_patch - never rewrite the headers above. -->

### Wave 1 — 关键重写 (4 todos)

- [x] 1. 重写 guide/core.md — GameWorld + GameWorldDriver + 固定步长逻辑帧
  What to do / Must NOT do: 完全替换现有内容。描述 GameWorld（纯 C# 驱动根，持有所有模块实例）、GameWorldDriver（MonoBehaviour，Unity 生命周期桥接）、HNLogicTime（静态纯数据类）、HNUnityFrameworkGlobalSettings（ScriptableObject 配置）、ITickable 接口。代码示例使用 `GameWorldDriver` 继承模式。Must NOT 保留任何 `HNUnityFramework` 抽象类的描述。
  Parallelization: Wave 1 | Blocked by: none | Blocks: 5,6,7,8
  References: 架构~/最终架构.md:433-500（GameWorld 双组件模式）, 架构~/最终架构.md:506-511（HNLogicTime 保留静态理由）, Runtime/HN.Framework.Core/Driver/GameWorld/GameWorld.cs:1-50, Runtime/HN.Framework.Unity/Driver/Platform/GameWorldDriver.cs:1-44
  Acceptance criteria: 文件不包含 `HNUnityFramework`（除 history/迁移说明），包含 `GameWorld` 类说明 + `GameWorldDriver` 继承示例 + `world.LogProvider` 注入模式 + 固定步长逻辑帧工作原理
  QA scenarios: grep -c "HNUnityFramework" docs-site~/docs/guide/core.md 应为 0（不含迁移说明段落），grep "GameWorldDriver" 应命中 2 次以上
  Commit: Y | docs(guide): rewrite core.md for GameWorld+GameWorldDriver pattern

- [x] 2. 重写 guide/quick-start.md — 新入口模式的完整上手示例
  What to do / Must NOT do: 用 GameWorldDriver 继承替换 HNUnityFramework 继承。示例：GameEntry : GameWorldDriver，重写 OnRegisterGameModules 注册模块。Procedure 示例改为 `world.ProcedureManager.AddProcedureState`。资源加载保持 Addressables 原生 API。Must NOT 使用 `ProcedureManager.ChangeProcedureState` 静态调用。
  Parallelization: Wave 1 | Blocked by: none | Blocks: none
  References: 架构~/最终架构.md:486-500（Scripts 仓库的 GameEntry 示例）, Runtime/HN.Framework.Unity/Driver/Platform/GameWorldDriver.cs:15-43
  Acceptance criteria: 代码示例使用 `GameWorldDriver` 继承，`world.ProcedureManager` 实例模式，`world.PoolManager` 实例模式。不包含静态 `.Instance` 访问。
  QA scenarios: grep "GameWorldDriver" quick-start.md 应命中，grep "\.Instance" quick-start.md 应为 0
  Commit: Y | docs(guide): rewrite quick-start for GameWorldDriver pattern

- [x] 3. 重写 api/index.md — 三程序集架构层分类
  What to do / Must NOT do: 完全替换模块列表，按三程序集组织：
  - HN.Framework.Core: Driver (GameWorld, HNLogicTime, ITickable, IReference, ReferencePool, PoolBase, ObjectPool<T>, PooledCollections), Capability (IAssetManager, ILogProvider, INetworkManager, IStorageProvider, ProcedureManager, ProcedureState, ObjectPoolManager), Level.Logic (MVC: Controller/Model/ControllerUnit/ModelUnit/ControllerManager; HFSM: HFSM/HFSMState/HFSMTransition/HFSMCompoundState; Entity 🚧; Sheet 🚧)
  - HN.Framework.Unity: Driver.Platform (GameWorldDriver, AddressablesOperator, ResourcesOperator, AssetDatabaseOperator, AssetManager, AssetCacheItem, AsyncLoadHandle, GameObjectPool, PooledObject<T>, UnityLogProvider, UnityTimeProvider, UnityCoroutineProvider, HNDictionary, SerializableDictionary, HNUnityFrameworkGlobalSettings, HNRenderPipeline 🚧), Capability (FishNet* 🚧, SheetElementTypeAttribute 🚧), Level.View (ViewFactory 🚧, EntityView 🚧, PropertyBinder 🚧)
  - HN.Framework.Editor: Core, Sheet, ObjectPool, AddressablesExtensions, Utils
  Must NOT 保留旧的 Runtime/xxx 平铺分类。
  Parallelization: Wave 1 | Blocked by: none | Blocks: none
  References: 架构~/最终架构.md:148-152（程序集清单）, 架构~/最终架构.md:407-419（命名空间→程序集映射）, 架构~/最终架构.md:621-645（模块状态一览）
  Acceptance criteria: 模块列表包含 `GameWorld`, `GameWorldDriver`, `IAssetManager`, `UnityTimeProvider`, `UnityCoroutineProvider`。按三个程序集分组，🚧 模块标注清楚。
  QA scenarios: grep -c "GameWorld" api/index.md ≥ 1, grep -c "GameWorldDriver" ≥ 1, grep "Core/" api/index.md 应为 0（不再有旧目录结构引用）
  Commit: Y | docs(api): restructure index by 3 assemblies with arch layers

- [x] 4. 重写 dev/architecture.md — 基于最终架构.md 浓缩版
  What to do / Must NOT do: 完全替换为浓缩版（目标 ~200 行），结构：
  - 三层架构全景（DriverLayer → CapabilityModule → Level）
  - 四仓库菱形依赖图
  - 三程序集依赖关系 + asmdef 规则
  - GameWorld 双组件模式（Core 纯 C# + Unity Driver）
  - 核心设计决策摘要（ReferencePool 为何静态、FishNet 隔离、Addressables 策略、UNITY_SERVER 宏）
  - 模块状态表（✅ / 🚧）
  - 末尾链接到完整 架构~/最终架构.md
  Must NOT 保留旧的生命周期图、旧的平铺目录树、旧的模块依赖图（以 ObjectPool/Procedure/MVC 为中心的那个）。
  Parallelization: Wave 1 | Blocked by: none | Blocks: 10
  References: 架构~/最终架构.md（全文 657 行，选核心内容浓缩）, docs-site~/docs/dev/architecture.md:1-145（当前完全过时的内容）
  Acceptance criteria: 包含 "GameWorld" 和 "GameWorldDriver" 和 "三层架构" 和 "四仓库" 关键词。不包含旧架构特征（如 Runtime/Core/ 平铺目录、HNUnityFramework 抽象类、ObjectPoolManager.Instance）。
  QA scenarios: grep "GameWorld" architecture.md 应命中, grep "DriverLayer" 应命中, grep "Runtime/Core/" architecture.md 应为 0
  Commit: Y | docs(dev): rewrite architecture.md from 最终架构.md (condensed)

### Wave 2 — 高优更新 (6 todos)

- [x] 5. 重写 guide/index.md — 三层架构概述 + 新模块导航
  What to do / Must NOT do: 替换模块列表为三层架构导航：
  - 驱动层：GameWorld、GameWorldDriver、基础类库（HNLogicTime/ReferencePool/序列化）、对象池（C# + GameObject）
  - 通用能力层：资源管理（IAssetManager/AddressablesOperator）、日志（ILogProvider/UnityLogProvider）、网络（INetworkManager 🚧）、存储（IStorageProvider）、流程管理（ProcedureManager）、事件（IEventBus 🚧）
  - 关卡层：MVC 框架、HFSM、Entity 系统 🚧、视图工厂 🚧
  - 编辑器工具
  保留框架概述段落。Must NOT 使用旧的平铺模块列表格式。
  Parallelization: Wave 2 | Blocked by: 1 | Blocks: none
  References: 架构~/最终架构.md:95-141（三层架构模型 + 框架提供 vs 上层仓库提供）
  Acceptance criteria: 模块列表包含 "GameWorld", "GameWorldDriver", "IAssetManager", "CapabilityModule"。旧名称 "Core 模块" 不再作为顶层分类。
  QA scenarios: grep -c "GameWorld" index.md ≥ 2, grep "CapabilityModule" index.md 应命中
  Commit: Y | docs(guide): rewrite index with 3-tier architecture overview

- [x] 6. 更新 guide/install.md — 新目录结构验证
  What to do / Must NOT do: 替换 "安装后验证" 部分的目录结构树，改为基于三个 asmdef 的新结构：
  ```
  Runtime/HN.Framework.Core/   （Driver/, Capability/, Level/）
  Runtime/HN.Framework.Unity/  （Driver/Platform/, Capability/, Level/View/）
  Editor/                      （Core/, Sheet/, ObjectPool/, AddressablesExtensions/, Utils/）
  ```
  其余部分（安装方式、环境要求）保持不变。
  Parallelization: Wave 2 | Blocked by: 1 | Blocks: none
  References: 架构~/最终架构.md:193-390（完整目录结构）
  Acceptance criteria: 目录树包含 `HN.Framework.Core/` 和 `HN.Framework.Unity/` 子目录，不包含旧的 `Runtime/Core/`, `Runtime/MVC/` 样式。
  QA scenarios: grep "HN.Framework.Core" install.md 应命中, grep "Runtime/Core/" install.md 应为 0
  Commit: Y | docs(guide): update install.md directory structure verification

- [x] 7. 更新 guide/mvc.md — 消除静态单例，使用实例化模式
  What to do / Must NOT do: 将 `ControllerManager.RegisterController()` 改为 `world.ControllerManager.RegisterController()`。注册示例中通过 `GameWorld` 访问。架构概览图中标注 ControllerManager 由 GameWorld 持有。其余内容（Model/Controller/Unit 生命周期、代码示例结构）保持不变，仅改 API 调用方式。
  Parallelization: Wave 2 | Blocked by: 1 | Blocks: none
  References: 架构~/最终架构.md:437-445（静态单例消除表）, Runtime/HN.Framework.Core/Driver/GameWorld/GameWorld.cs:13
  Acceptance criteria: 不包含 `ControllerManager.Instance` 或 `ControllerManager.RegisterController`（无 world 前缀）。代码示例通过 `world.ControllerManager.xxx` 访问。
  QA scenarios: grep "\.Instance" mvc.md 应为 0, grep "world.ControllerManager" mvc.md 应命中
  Commit: Y | docs(guide): update mvc.md for instance-based ControllerManager

- [x] 8. 更新 guide/object-pool.md — 消除静态单例
  What to do / Must NOT do: 将 `ObjectPoolManager.Initialize()` 改为 `world.PoolManager`（实例由 GameWorld 构造时自动创建）。将 `ObjectPoolManager.CreateObjectPool<T>()` 改为 `world.PoolManager.CreateObjectPool<T>()`。架构概览图标注由 GameWorld 持有。其余内容（池类型、配置参数、自动调节）保持不变。
  Parallelization: Wave 2 | Blocked by: 1 | Blocks: none
  References: 架构~/最终架构.md:513-526（ObjectPool 拆分边界）, Runtime/HN.Framework.Core/Driver/GameWorld/GameWorld.cs:11
  Acceptance criteria: 不包含 `ObjectPoolManager.Initialize()` 静态调用。代码示例使用 `world.PoolManager.xxx`。
  QA scenarios: grep "ObjectPoolManager.Initialize" object-pool.md 应为 0, grep "world.PoolManager" object-pool.md 应命中
  Commit: Y | docs(guide): update object-pool.md for instance-based pool manager

- [x] 9. 更新 guide/procedure.md — 消除静态单例
  What to do / Must NOT do: 将 `ProcedureManager.AddProcedureState<T>()` 改为 `world.ProcedureManager.AddProcedureState<T>()`。将 `ProcedureManager.ChangeProcedureState()` 改为 `world.ProcedureManager.ChangeProcedureState()`。架构概览标注由 GameWorld 持有。其余内容（事件驱动、生命周期、与 HFSM 对比）保持不变。
  Parallelization: Wave 2 | Blocked by: 1 | Blocks: none
  References: 架构~/最终架构.md:441, Runtime/HN.Framework.Core/Driver/GameWorld/GameWorld.cs:12
  Acceptance criteria: 不包含直接的 `ProcedureManager.AddProcedureState` 调用（无 world 前缀）。代码示例通过 `world.ProcedureManager.xxx` 访问。
  QA scenarios: grep "ProcedureManager\." procedure.md（不含 world. 前缀的调用）应 ≤ 1（仅在说明性文本中）, grep "world.ProcedureManager" 应命中
  Commit: Y | docs(guide): update procedure.md for instance-based ProcedureManager

- [x] 10. 更新 dev/code-standard.md — 命名空间表
  What to do / Must NOT do: 替换命名空间表为：
  | 命名空间 | 适用代码 |
  | HN.Framework.Core.Driver | GameWorld, GameWorld 基础类库 |
  | HN.Framework.Core.Driver.Common | ITickable, IReference, HNLogicTime, 序列化, 池系统 |
  | HN.Framework.Core.Capability | Capability 接口定义（IAssetManager, ILogProvider, INetworkManager, IStorageProvider, ProcedureManager, ObjectPoolManager） |
  | HN.Framework.Core.Level.Logic | MVC, HFSM, Entity |
  | HN.Framework.Unity.Driver.Platform | Unity 平台适配（GameWorldDriver, AddressablesOperator, GameObjectPool, UnityLogProvider 等） |
  | HN.Framework.Unity.Capability | FishNet 封装, Sheet 运行时 |
  | HN.Framework.Unity.Level.View | ViewFactory, EntityView, PropertyBinder |
  | HN.Framework.Editor | Editor 工具 |
  Must NOT 保留旧的 `HN.Framework`、`HN.Serialize`、`HN` 命名空间在表中。
  Parallelization: Wave 2 | Blocked by: 4 | Blocks: none
  References: 架构~/最终架构.md:405-419（命名空间规范）, 架构~/最终架构.md:424-429（已知不一致）
  Acceptance criteria: 命名空间表不包含 `HN.Framework`（不含子命名空间）或 `HN.Serialize` 或孤立的 `HN`。包含 `HN.Framework.Core.*` 和 `HN.Framework.Unity.*` 条目。
  QA scenarios: grep "HN\.Framework$" code-standard.md 应为 0, grep "HN\.Framework\.Core" code-standard.md 应命中
  Commit: Y | docs(dev): update code-standard namespace table

### Wave 3 — 低优命名空间更新 (5 todos)

- [x] 11. 更新 guide/reference-pool.md — 命名空间
  What to do / Must NOT do: 更新 using 语句从 `using HN.Framework;` 到 `using HN.Framework.Core.Driver.Common.Pool.ReferencePool;`。更新代码示例中的 using。内容本身正确（ReferencePool 仍是静态类），不做结构变更。
  Parallelization: Wave 3 | Blocked by: none | Blocks: none
  References: 架构~/最终架构.md:231-233
  Acceptance criteria: using 示例指向新命名空间
  QA scenarios: grep "using HN\.Framework;" reference-pool.md 应为 0（除非在迁移说明中）
  Commit: Y | docs(guide): update reference-pool namespace

- [x] 12. 更新 guide/hfsm.md — 命名空间
  What to do / Must NOT do: 更新 using 语句。HFSM 类在 `HN.Framework.Core.Level.Logic`。代码示例中的 `using HN.Framework;` 改为 `using HN.Framework.Core.Level.Logic;`。内容基本正确，不做结构变更。
  Parallelization: Wave 3 | Blocked by: none | Blocks: none
  References: grep output showing HFSM files use `namespace HN.Framework.Core.Level.Logic`
  Acceptance criteria: using 示例指向 `HN.Framework.Core.Level.Logic`
  QA scenarios: grep "using HN\.Framework\.Core\.Level\.Logic" hfsm.md 应命中
  Commit: Y | docs(guide): update hfsm namespace

- [x] 13. 更新 guide/serialize.md — 命名空间 + 拆分说明
  What to do / Must NOT do: 更新 using 语句。说明序列化模块拆分为 Core 层（Json.cs, JsonObject.cs）和 Unity 层（JsonData.cs）。update namespace: `HN.Framework.Core.Driver.Common.Serialization` (Core) + `HN.Framework.Unity.Driver.Platform.Serialization` (Unity).
  Parallelization: Wave 3 | Blocked by: none | Blocks: none
  References: 架构~/最终架构.md:218-223（Core 序列化）, 架构~/最终架构.md:315-317（Unity 序列化）
  Acceptance criteria: 不包含 `HN.Serialize` 命名空间引用，包含 "Core 层" 和 "Unity 层" 拆分说明
  QA scenarios: grep "HN\.Serialize" serialize.md 应为 0
  Commit: Y | docs(guide): update serialize namespace and split note

- [x] 14. 更新 guide/utils.md — 命名空间
  What to do / Must NOT do: 更新命名空间从 `HN` 到 `HN.Framework.Unity.Driver.Platform.DataStructures`。更新 using 语句和代码示例。
  Parallelization: Wave 3 | Blocked by: none | Blocks: none
  References: grep output showing HNDictionary uses `namespace HN.Framework.Unity.Driver.Platform.DataStructures`
  Acceptance criteria: 不包含 `using HN;`，使用 `using HN.Framework.Unity.Driver.Platform.DataStructures;`
  QA scenarios: grep "using HN;" utils.md 应为 0
  Commit: Y | docs(guide): update utils namespace

- [x] 15. 更新 guide/asset-manager.md — CapabilityModule 定位
  What to do / Must NOT do: 在文件开头添加 CapabilityModule 架构说明：`IAssetManager` 接口在 Core 层、`AssetManager` 实现 + `AddressablesOperator` 等在 Unity 层。保留已弃用说明和 Addressables 原生 API 推荐。更新 using 示例。加入 `IAssetManager` 和三种操作器的架构关系图。
  Parallelization: Wave 3 | Blocked by: none | Blocks: none
  References: 架构~/最终架构.md:565-572（Addressables 资源策略）, Runtime/HN.Framework.Core/Capability/Asset/IAssetManager.cs:4
  Acceptance criteria: 包含 `IAssetManager` 接口说明和 CapabilityModule 定位，包含架构图
  QA scenarios: grep "IAssetManager" asset-manager.md 应命中, grep "CapabilityModule" asset-manager.md 应命中
  Commit: Y | docs(guide): update asset-manager.md for CapabilityModule positioning

### Wave 4 — 新增占位文档 (6 todos)

- [x] 16. 新增 guide/capability.md — CapabilityModule 服务接口总览 🚧
  What to do / Must NOT do: 创建 overview 页面描述 CapabilityModule 层提供的服务接口。列出所有接口及其实现状态：ILogProvider ✅, IAssetManager ✅, INetworkManager 🚧, IStorageProvider ✅, IEventBus 🚧。每个接口 1-2 句描述 + 实现状态标记。Must NOT 为 Stub 接口编造 API 细节。
  Parallelization: Wave 4 | Blocked by: none | Blocks: none
  References: 架构~/最终架构.md:111-116（CapabilityModule 层描述）, 架构~/最终架构.md:138-139（框架提供的 Capability 接口）
  Acceptance criteria: 文件存在，列出所有 Capability 接口并标注 ✅/🚧，sidebar_position: 13
  QA scenarios: grep "INetworkManager" capability.md 应命中, grep "ILogProvider" capability.md 应命中
  Commit: Y | docs(guide): add capability.md service interface overview

- [x] 17. 新增 guide/entity.md — Entity 系统 🚧 Stub
  What to do / Must NOT do: 创建占位页面。内容：Entity 系统的架构定位（Level.Logic 层，纯数据实体基类 + EntityManager 生命周期管理）。标记 🚧 状态，链接到架构文档。不超过 20 行内容。Must NOT 编造 API。
  Parallelization: Wave 4 | Blocked by: none | Blocks: none
  References: 架构~/最终架构.md:283-286（Entity 🚧 Stub）
  Acceptance criteria: sidebar_position: 14，包含 🚧 标记，≤ 20 行实际内容
  QA scenarios: Read entity.md, verify it contains 🚧 and architecture references
  Commit: Y | docs(guide): add entity.md stub

- [x] 18. 新增 guide/view-factory.md — ViewFactory/EntityView/PropertyBinder 🚧 Stub
  What to do / Must NOT do: 创建占位页面。内容：ViewModule 架构定位（Level.View 层，Unity 依赖），三个类的角色（ViewFactory 视图工厂基类、EntityView 实体视图基类 MonoBehaviour、PropertyBinder 属性绑定）。标记 🚧 状态。不超过 25 行。
  Parallelization: Wave 4 | Blocked by: none | Blocks: none
  References: 架构~/最终架构.md:352-356（ViewModule 🚧 Stub）
  Acceptance criteria: sidebar_position: 15，包含 🚧 标记
  QA scenarios: grep "ViewFactory" view-factory.md 应命中
  Commit: Y | docs(guide): add view-factory.md stub

- [x] 19. 新增 guide/network.md — INetworkManager + FishNet 封装 🚧 Stub
  What to do / Must NOT do: 创建占位页面。内容：INetworkManager 接口（Core 层定义）、FishNet 四件套封装（FishNetNetworkManager, FishNetMessageBus, FishNetConnectionAdapter, FishNetSerializerAdapter）。FishNet 策略表（✅直接传输 / ❌不用 NetworkBehaviour/SyncVar/RPC）。标记 🚧 状态。不超过 30 行。
  Parallelization: Wave 4 | Blocked by: none | Blocks: none
  References: 架构~/最终架构.md:529-555（FishNet 封装策略）
  Acceptance criteria: sidebar_position: 16，包含 FishNet 策略表
  QA scenarios: grep "FishNetNetworkManager" network.md 应命中
  Commit: Y | docs(guide): add network.md fishnet stub

- [x] 20. 新增 guide/sheet.md — Sheet 配置表 + Luban 🚧 Stub
  What to do / Must NOT do: 创建占位页面。内容：Luban（外部工具）与 Sheet 编辑器（框架）的协作模式概述。工作流程图：Excel → Luban 生成 C#+二进制 → Sheet 编辑器编辑资源引用 → 运行时加载。标记 🚧 状态。不超过 25 行。
  Parallelization: Wave 4 | Blocked by: none | Blocks: none
  References: 架构~/最终架构.md:557-563（Luban + Sheet 协作模式）
  Acceptance criteria: sidebar_position: 17，包含 Luban+Sheet 工作流描述
  QA scenarios: grep "Luban" sheet.md 应命中
  Commit: Y | docs(guide): add sheet.md luban stub

- [x] 21. 新增 guide/rendering.md — HNRenderPipeline SRP 🚧 Stub
  What to do / Must NOT do: 创建占位页面。内容：HNRenderPipeline（SRP 核心代码框架提供）+ ShaderLibrary（Core/PBR/Shadows/PostProcess hlsl）+ Art 仓库项目 Shader（基于 ShaderLibrary 编写）。SRP 分工表。标记 🚧 状态。不超过 25 行。
  Parallelization: Wave 4 | Blocked by: none | Blocks: none
  References: 架构~/最终架构.md:327-334（Rendering 🚧 Stub）, 架构~/最终架构.md:581-587（SRP 分工）
  Acceptance criteria: sidebar_position: 18，包含 ShaderLibrary 四个文件列表
  QA scenarios: grep "ShaderLibrary" rendering.md 应命中, grep "HNRenderPipeline" rendering.md 应命中
  Commit: Y | docs(guide): add rendering.md srp stub

## Final verification wave
> Runs in parallel after ALL todos. ALL must APPROVE. Surface results and wait for the user's explicit okay before declaring complete.

- [x] F1. Plan compliance audit — 验证所有 21 个 todo 完成
  What: 遍历 todo 1-21，确认每个文件存在且通过各自的 QA scenarios
  QA: 对每个文件执行对应的 grep 验证命令

- [x] F2. Cross-reference consistency — API 名称一致性
  What: 检查 quick-start.md 的代码示例引用的 API 与 core.md / api/index.md 中描述的名称一致
  QA: 交叉对比三个文件的类名和 API 调用

- [x] F3. Old API leakage scan — 全局旧 API 残留搜索
  What: grep 搜索所有更新文件：`HNUnityFramework`（旧类名）、`\.Instance`（静态单例）、`Runtime/Core/`（旧目录）、`HN\.Serialize`（旧命名空间）、`using HN;`（旧根命名空间）
  QA: 所有命中仅在 architecture.md 迁移对比段或被明确标记为"旧版"的段落中

- [x] F4. Scope boundary check — 确认未修改不应修改的文件
  What: `git diff --name-only` 确认只修改了 docs-site~/docs/ 下的 .md 文件
  QA: 变更文件列表不包含 .cs / .ts / .json / docusaurus.config.ts

## Commit strategy
- 每波更新完成后提交一次（4 commits，对应 Wave 1-4）
- 格式遵循 `docs(<scope>): <description>`
- Wave 1: `docs(guide,api,dev): rewrite critical docs for 3-tier architecture`
- Wave 2: `docs(guide,dev): update high-priority docs for instance-based pattern`
- Wave 3: `docs(guide): update namespace references in low-priority docs`
- Wave 4: `docs(guide): add stub placeholder docs for unimplemented modules`

## Success criteria
1. ✅ docs/guide/core.md 描述 GameWorld + GameWorldDriver，不含 HNUnityFramework
2. ✅ docs/guide/quick-start.md 代码示例使用 GameWorldDriver 继承模式
3. ✅ docs/api/index.md 按三程序集+架构层分类
4. ✅ docs/dev/architecture.md 反映三层架构，不含旧扁平结构
5. ✅ 所有 guide 文档的 using 语句使用新命名空间
6. ✅ 所有 manager 访问使用实例化模式（world.XxxManager）
7. ✅ 6 个新占位文档存在（capability/entity/view-factory/network/sheet/rendering），均标注 🚧
8. ✅ 15 个现有文档完成更新，不含旧 API 代码残留
